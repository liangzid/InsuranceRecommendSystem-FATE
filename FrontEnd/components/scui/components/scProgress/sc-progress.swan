<view class="sc-class sc-progress"><view class="sc-progress-bg" style="background-color: {{color}};height: {{size}}rpx"></view><view s-if="{{type === 'indeterminate'}}" class="sc-progress-indeterminate-bar" style="background-color: {{color}}"></view><view s-if="{{type === 'determinate'}}" class="sc-progress-determinate-bar" style="background-color: {{color}};width: {{width > 100 ? 100 : width}}%"></view></view>